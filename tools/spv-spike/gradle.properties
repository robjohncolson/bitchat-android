# Dev-only SPV feasibility spike. Compiles + runs the app on JDK 17 via the Gradle
# toolchain in build.gradle (auto-detected). Gradle itself may run on any JDK it supports.
org.gradle.jvmargs=-Xmx2g
